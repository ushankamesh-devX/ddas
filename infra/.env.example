# Local Docker infrastructure only. Do not reuse these credentials elsewhere.
POSTGRES_DB=dam_alert
POSTGRES_USER=damapp
POSTGRES_PASSWORD=damapp_dev

# MinIO administrator account used to initialize the local container.
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin_dev_only
