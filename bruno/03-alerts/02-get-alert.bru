meta {
  name: Get Alert
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/dams/{{damId}}/alerts/{{alertId}}
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("returns success", function () {
    expect(res.getStatus()).to.be.oneOf([200, 201, 204]);
  });
}
