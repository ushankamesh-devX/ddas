meta {
  name: Publish Citizen Report
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/dams/{{damId}}/reports/{{reportId}}/publish
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("returns success", function () {
    expect(res.getStatus()).to.be.oneOf([200, 201, 204]);
  });
}
