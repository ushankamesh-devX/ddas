meta {
  name: Civilian cannot publish news
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/dams/{{damId}}/news/{{newsId}}/publish
}

headers {
  Authorization: Bearer {{civilianToken}}
}

tests {
  test("status is 403", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
