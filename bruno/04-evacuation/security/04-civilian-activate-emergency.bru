meta {
  name: Civilian cannot activate emergency
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/dams/{{damId}}/emergency/activate
  body: json
}

headers {
  Authorization: Bearer {{civilianToken}}
  Content-Type: application/json
  Idempotency-Key: civilian-emergency
}

body:json {
  {
  "reason": "No"
  }
}

tests {
  test("status is 403", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
