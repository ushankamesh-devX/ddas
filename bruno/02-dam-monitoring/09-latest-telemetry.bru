meta {
  name: Latest Telemetry
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/api/v1/dams/{{damId}}/telemetry/latest
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("returns success", function () {
    expect(res.getStatus()).to.be.oneOf([200, 201, 204]);
  });
}
