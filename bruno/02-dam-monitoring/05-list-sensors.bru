meta {
  name: List Sensors
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/dams/{{damId}}/sensors
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("returns success", function () {
    expect(res.getStatus()).to.be.oneOf([200, 201, 204]);
  });
}
