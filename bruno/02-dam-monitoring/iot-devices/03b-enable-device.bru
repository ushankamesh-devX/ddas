meta {
  name: Enable IoT Device
  type: http
  seq: 3.2
}

post {
  url: {{baseUrl}}/api/v1/dams/{{damId}}/iot-devices/{{iotDeviceId}}/enable
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("device enabled", function () { expect(res.getStatus()).to.equal(204); });
}
