meta {
  name: Revoke IoT Device
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/v1/dams/{{damId}}/iot-devices/{{iotDeviceId}}/revoke
  body: json
}

headers {
  Authorization: Bearer {{accessToken}}
  Content-Type: application/json
}

body:json {
  {
  "reason": "Credential compromise test"
  }
}

tests {
  test("device revoked", function () {
    expect(res.getStatus()).to.be.oneOf([200,204]);
  });
}
