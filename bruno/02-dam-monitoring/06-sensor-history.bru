meta {
  name: Sensor History
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/v1/dams/{{damId}}/sensors/{{sensorId}}/readings
}

headers {
  Authorization: Bearer {{accessToken}}
}

tests {
  test("returns success", function () {
    expect(res.getStatus()).to.be.oneOf([200, 201, 204]);
  });
}
