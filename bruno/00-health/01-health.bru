meta {
  name: Health
  type: http
  seq: 1
  tags: [
    foundation
  ]
}

get {
  url: {{baseUrl}}/actuator/health
}

tests {
  test("service is healthy", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
