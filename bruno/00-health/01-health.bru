meta {
  name: Health
  type: http
  seq: 1
  tags: [
    smoke
  ]
}

get {
  url: {{baseUrl}}/actuator/health
}

tests {
  test("service is healthy", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
