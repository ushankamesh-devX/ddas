# Feature modules

Keep domain-specific UI, state, and types in folders here, for example `dams/`,
`monitoring/`, `alerts/`, `evacuation/`, `reports/`, and `news/`.

Shared UI belongs in `src/components/`; API and authentication infrastructure
belongs in `src/lib/`.
